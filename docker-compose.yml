services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - .:/app
    command: >
      bash -lc "pip install -r requirements-dev.txt &&
      jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root"
